#! /usr/bin/env sh
set -euo pipefail

export REQUIREMENTS="poetry.lock"

export SETUP_CONFIG="pyproject.toml"
export SERVICE_NAME='albatross'
export POETRY_VERSION='2.1.2'

export PYTHONPATH="${WORKSPACE_DIR}/src:${WORKSPACE_DIR}"
export GITHUB_REMOTE_URL="git@github.com:mpb-com/albatross"
export MYPYPATH="${WORKSPACE_DIR}"%